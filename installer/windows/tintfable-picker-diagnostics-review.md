# Native Save picker refusal evidence — 2026-09-12

This candidate repairs missing diagnostic evidence. It **does not claim to fix the unknown Save picker topology**, accept a new control ID, or complete the installed consumer workflow. Base source is `9dd4249f7a3245627b1892e4900e48dde9269283`; the observed Windows run is `34679755050`, public source `f0f4921cbb4b316135ae53822bd89584b5e59a81`.

The full workflow receipt and original `consumer-failure.png` were inspected under `/private/tmp/tintfable-34679755050-review/pixelquay/pixelquay/build-evidence/msix-install`. The installed TintFable 1.0.1.0 app ran in PID 5760, retained main HWND 589942, with 205 verified modules. Its Open picker HWND 1114412, title `Open Image File`, ownership chain `[1114412,589942]` passed native filename focus (Edit HWND 852494), exact 1148 ancestry, writable state, bounded text readback and submission. The original image opened and the actual image rotation completed, with real screenshots. Display preparation and placement produced the requested visible 1472×940 app window on the actual 1920×1080 display.

The failure is the first filename observation in the subsequent `Save Image File` picker, HWND 983496, ownership chain `[983496,589942]`, same PID. Its screenshot shows `source` selected in the filename box, PNG type, and Save/Cancel. It does not establish the actual Win32 focus HWND, Active HWND, control class/ID chain or style. The receipt contains only the **combined** `ValidateFileName` exception, no failed filename evidence, and no picker tree. The successful Open record is the only `picker_fields` entry. The final exact mismatch cannot be determined from these artifacts; neither a different Save control ID nor a focus/active-window race is proven.

The bounded change records evidence at that existing production refusal:

- Each unchanged filename predicate reports its name (for example `active_window`, `filename_id_1148`, `edit_class`, `retained_focus`) and attaches its actual evidence to the C# exception.
- The native observer retains initial focus/active HWNDs, expected focus, PIDs and thread IDs, raw style, existence/visibility/enabled/descendant/writable state, and up to 16 actual ancestor HWNDs with parent, PID, class and control ID. It reports whether the bounded walk reached the exact dialog. The final focus reread is retained separately when reached. These diagnostics do not introduce an alternate accepted topology.
- PowerShell preserves the original exception while extracting its attached evidence through at most eight exception wrappers. `consumer-workflow.json` gains a bounded `picker_failures` record with the exact input stage, title/HWND/PID and retained focus. Successful picker observations also retain the native evidence for an Open/Save comparison. No new text/path scraping, input action, retry, or focus correction is performed by diagnostics.
- Failure to record diagnostics is reported separately and cannot replace the primary input refusal. The production Alt+N → observed filename → Ctrl+A → Unicode text → exact WM_GETTEXT readback → Enter sequence remains unchanged. Filename-specific APIs still pass expected focus into the final SendInput boundary. The exact live retained process/window/title/foreground/ownership chain, class Edit, writable style and 1148 ancestor conditions remain mandatory.

Local verification completed with the existing PowerShell 7.6.6 runtime at `/Users/hashfunction/workspace/project_app_factory/microsoft-store/apps/filequay/source/.tools/powershell-7.6.6/pwsh`, `TMPDIR=/private/tmp`, and that runtime directory on PATH:

- `installer/windows/test_consumer_input.ps1`: 19 process/window/pre-input cases, 14 native filename predicate cases and GUI/INPUT ABI checks; existing final SendInput ordering, partial-delivery, style-zero/read-only and retained-focus API checks; new real C# exception → PowerShell → JSON preservation of failed predicates and native chain/style/thread fields; alternate filename IDs still refused; 16-record diagnostic bound.
- `installer/windows/test_consumer_native_picker.ps1`: eight production picker sequencing/refusal cases, including the newly covered first observation after Alt+N. That failure sends no Ctrl+A, text or Enter and acquires no retained focus. All failure stages survive JSON serialization. A separate diagnostic-recording failure still preserves the original native refusal and sends no extra input.
- All **12** PowerShell fixtures listed in `installer/windows/qualify-msix.ps1` passed in separate processes, including geometry/display, picker selection, unpackaged profile lifecycle, orchestration, registration ownership, source/package evidence, SDK unpack and temporary ownership.
- `python3 -m unittest discover -s installer/windows -p 'test_*.py' -v`: **70 passed, one Windows junction test skipped**. This includes all 24 independent original PNG/rotation/recipe/export/reopen/cleanup cases. Product/fixture bytes and those acceptance predicates were not changed.
- C# compilation through Add-Type, PowerShell syntax and `git diff --check` passed. No app rebuild or download was needed locally.

The actual run still has `workflow_acceptance=false`, `export_workflow_tested=false`, `clean_close_verified=false`, and `uninstall_verified=false`. Its cleanup stopped all owned processes, removed the consumer fixture and left no package registrations or cleanup errors; the separate diagnostic launch had a clean close. Those cleanup facts do not establish the failed consumer lifecycle.

Next action: run the existing Windows qualification against this exact candidate. Inspect the Save picker’s `picker_failures[].stage`, `native.FailedPredicates`, native focus/active fields and ancestor array before proposing any topology correction. The full actual save, recipe, export, reopened pixels, normal zero close and owned cleanup remain required. No Store readiness or public consumer screenshot acceptance is claimed from this diagnostic candidate.
