# Exact source license supplements

Windows qualification found no installed license file for the GStreamer bad-plugins libraries split package. The license here was extracted from the upstream archive inside the exact MSYS2 1.28.7-1 source package. Archive and license hashes, the packaging recipe, and generated package metadata are retained in sources.json.

The inventory accepts this text only when package name, version, declared license and upstream URL match, and the license bytes match the recorded hash. Missing files declared by the installed package still fail. Unrelated packages and later versions receive no exception. Native corresponding-source delivery remains a separate release gate.
